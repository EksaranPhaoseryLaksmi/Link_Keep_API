server:
  address: 0.0.0.0
  port: ${PORT:8080}

spring:
  datasource:
    url: jdbc:mysql://crossover.proxy.rlwy.net:23617/railway?sslMode=DISABLED&allowPublicKeyRetrieval=true&autoReconnect=true
    username: root
    password: IJRFHiyTaYXpvUIvqBuWzgFFifwoCtzm
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      connection-timeout: 30000
      validation-timeout: 5000
      maximum-pool-size: 5
      initialization-fail-timeout: -1

  mail:
    host: smtp.gmail.com
    port: 587
    username: darabuntha1996@gmail.com
    password: elucvgpundjloevn
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.gmail.com

app:
  base:
    url: https://link-keep-api.onrender.com

mybatis:
  type-aliases-package: com.example.mywebapp.entity
  mapper-locations: classpath:mapper/*.xml